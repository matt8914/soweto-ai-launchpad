SQLITE_PATH=.data/launchpad.sqlite

# Optional later extension only. The challenge-day starter works without an API key.
# Use this only if you intentionally replace the deterministic local coach stub.
# OPENAI_API_KEY=
